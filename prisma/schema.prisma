generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:root@localhost:5432/employee-managment-TS?schema=public"
}

model Role {
  id   Int    @id @default(autoincrement())
  role role
  User User[]
}

model User {
  id           String          @id @default(uuid())
  name         String          @db.VarChar(40)
  email        String          @unique @db.VarChar(65)
  password     String          @unique @db.VarChar(100)
  organization String          @db.VarChar(40)
  is_admin     Boolean         @default(false)
  role         Role            @relation(fields: [roleId], references: [id])
  roleId       Int
  created_at   DateTime        @default(now())
  updated_at   DateTime        @updatedAt
  deleted_at   DateTime?
  manager      user_reportee[] @relation("ManagerReport")
  reportee     user_reportee[] @relation("ReporteeReports")
}

enum role {
  Employee
  Manager
}

model user_reportee {
  id         String   @id @default(uuid())
  reporteeId String
  managerId  String
  reportee   User     @relation("ReporteeReports", fields: [reporteeId], references: [id])
  manager    User     @relation("ManagerReport", fields: [managerId], references: [id])
  created_at DateTime @default(now())
  updated_at DateTime @updatedAt
}
